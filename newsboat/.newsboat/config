browser "readable '%u' | pandoc -f html -t gfm | glow -p"
browser "\
is_image=$(echo %u | grep -E -c '.(jpg|jpeg|png|gif)');\
if [[ $is_image -eq 1 ]]; then curl %u | wezterm imgcat;read -n 1 -s;clear; \
else curl -L -v --cookie $COURRIER_COOKIE %u | readable | pandoc -f html -t gfm | glow -p; \
fi"
show-read-feeds no
confirm-mark-feed-read no
download-full-page yes
feed-sort-order lastupdated-desc
datetime-format "%d/%m"
prepopulate-query-feeds yes
max-items 25

urls-source "feedhq"
feedhq-url "https://bazqux.com/"
feedhq-login ""
feedhq-password ""
feedhq-flag-star "s"

# Interface
articlelist-title-format " %T (%u unread)"
feedlist-title-format " Feeds (%u unread, %t total)"
itemview-title-format " %T"
urlview-title-format " #"

feedlist-format "%n %3U %t"
articlelist-format "%f %D  %?T?%-15T  ?%t"

# highlight article "^(Feed|Link):.*$" yellow default bold
# highlight article "^(Title|Date|Author):.*$" yellow default bold
# highlight article "https?://[^ ]+" blue default underline
# highlight article "\\[[0-9]+\\]" yellow default bold
# highlight article "\\[image\\ [0-9]+\\]" yellow default bold
# highlight feedlist "^─.*$" yellow color235 bold

show-keymap-hint no
swap-title-and-hints no
# openbrowser-and-mark-jumps-to-next-unread yes

bind-key j down
bind-key k up
bind-key j next articlelist
bind-key k prev articlelist

bind-key o open-in-browser-and-mark-read
bind-key O open-in-browser

# open feed/article with spacebar
bind-key SPACE open

# star articles
unbind-key s
bind-key s edit-flags

# Open in browser
